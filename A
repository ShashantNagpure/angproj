    console.log(this.input.nativeElement.style.transition);
    var sectionHeight = this.input.nativeElement.scrollHeight;
    // var elementTransition = this.input.nativeElement.style.transition;
    // this.input.nativeElement.style.transition = '';
    this.input.nativeElement.style.height = sectionHeight + 'px';
    // this.input.nativeElement.style.transition = elementTransition;
    this.input.nativeElement.style.height = 0 + 'px';
      ngAfterViewInit() {
    console.log(this.input.nativeElement.scrollHeight);
    
  }
   @ViewChild('card') input;
   
       overflow:hidden;
    transition:height 5s ease-out;
    height:500px;
